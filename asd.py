'''inputElement = driver.find_element_by_class_name("gLFyf")
        inputElement.clear()
        dataToSend = data[0]+ " " + data[1] + " " + data[2]
        inputElement.send_keys(dataToSend)
        time.sleep(1)
        inputElement.send_keys(Keys.RETURN)'''